apiVersion: v1
kind: Service
metadata:
  name: health-ai
  namespace: health
  labels:
    app: health-ai
    app.kubernetes.io/name: health-ai
    app.kubernetes.io/component: ai
spec:
  selector:
    app: health-ai
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
