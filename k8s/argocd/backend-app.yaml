apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: health-backend-production
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: health-tracker
    environment: production
  finalizers:
    - resources-finalizer.argocd.io # cascade-delete managed resources on app removal
  annotations:
    notifications.argoproj.io/subscribe.on-sync-succeeded.slack: deployments
    notifications.argoproj.io/subscribe.on-sync-failed.slack: deployments
spec:
  project: health-tracker

  source:
    repoURL: https://gitlab.com/YOUR_GROUP/health-tracker-app.git # replace with your repo URL
    targetRevision: main
    path: k8s/overlays/production

  destination:
    server: https://kubernetes.default.svc
    namespace: health

  syncPolicy:
    automated:
      prune: true # remove resources deleted from git
      selfHeal: true # revert manual changes in cluster
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - RespectIgnoreDifferences=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  # Ignore live image tag diffs (ArgoCD compares against git, not live state)
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/template/metadata/annotations/kubectl.kubernetes.io~1last-applied-configuration

  revisionHistoryLimit: 10
